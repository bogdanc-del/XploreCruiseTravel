node_modules/
.next/
.env.local
*.tsbuildinfo

# Playwright
test-results/
artifacts/playwright-report/
blob-report/

# Scraper output (large files, not deployed)
scripts/output/
logs/

# Note: public/data/cruises*.json files are tracked by git intentionally.
# They are committed by the daily sync workflow and deployed to Vercel.
# Do NOT add them to .gitignore.
